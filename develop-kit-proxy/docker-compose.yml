version: '3'

services:
    proxy:
        container_name: proxy
        image: nginx:alpine
        ports:
            - "80:80"
        volumes:
            - ./config/conf.d:/etc/nginx/conf.d
            - ./config/nginx.conf:/etc/nginx/nginx.conf
            - ./src:/var/www/html
        networks:
            - databases_common
        restart: always
networks:
    databases_common:
        external: true
